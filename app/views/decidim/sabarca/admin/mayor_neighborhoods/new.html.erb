<%= decidim_form_for(@form, url: decidim_sabarca.admin_mayor_neighborhoods_path, html: { class: "form new_mayor_neighborhood" }) do |f| %>
  <div class="card">
    <div class="card-divider">
      <h2 class="card-title"><%= t("mayor_neighborhoods.new.title", scope: "decidim.admin.actions") %></h2>
    </div>
    <div class="card-section">
      <%= render partial: 'form', object: f %>
    </div>
  </div>
  <div class="button--double form-general-submit">
    <%= f.submit t(".create") %>
  </div>
<% end %>
